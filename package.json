{
  "name": "ru-whatsapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write .",
    "zip-package": "echo 'Zipping deployment package...' && zip -r ru-menu-whatsapp.zip index.js mongoAuthState.js helpers.js node_modules",
    "build": "npm install && npm run zip-package",
    "format:check": "prettier --check .",
    "locally": "node -e \"console.log(require('./index').handler(require('./event.json')));\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@whiskeysockets/baileys": "github:WhiskeySockets/Baileys#feature/newsletter",
    "dotenv": "^16.4.5",
    "mongodb": "^6.8.0",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
